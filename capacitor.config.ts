import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.project1111',
  appName: 'project1111',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
